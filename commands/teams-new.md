---
description: Scaffold a new domain team — creates domain file, agent stubs, and vault wiring.
---

Read the `teams-new` skill. Then run the scaffold workflow.

Usage:
- `/teams-new <domain>` — scaffold a domain (e.g., `/teams-new clientenv`)
- `/teams-new <domain> --agents python-engineer,network-engineer` — with specific agent roles
