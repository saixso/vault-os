---
name: REPLACE-skill-name
description: >
  REPLACE — One-sentence pitch. What does this skill do?
  Triggers on: "phrase 1", "phrase 2", "/REPLACE-skill-name".
allowed-tools: Read Write Edit Glob Grep
---

# REPLACE-skill-name: REPLACE Human-Readable Title

REPLACE — One paragraph explaining the purpose. Why does this skill exist? What problem does it solve?

---

## Workflow

1. REPLACE — First step (e.g., read input, gather context)
2. REPLACE — Core logic (e.g., process, transform, generate)
3. REPLACE — Output (e.g., write file, update index, report to user)

## Output Format

REPLACE — Describe what the skill produces:
- Files created/modified (paths, frontmatter schema)
- Messages shown to the user
- Side effects (index updates, log entries)

## Constraints

- REPLACE — What this skill must NOT do
- REPLACE — Token budget or scope limits
- REPLACE — Safety rails (e.g., never overwrite without confirmation)
