---
name: REPLACE-agent-name
description: >
  REPLACE — What this agent does. When to dispatch it.
  <example>Context: REPLACE scenario
  assistant: "REPLACE — what Claude says when dispatching this agent."
  </example>
model: sonnet
maxTurns: 30
tools: Read, Write, Edit, Glob, Grep
---

You are a REPLACE-role. Your job is to REPLACE-primary-purpose.

You will be given:
- REPLACE — Input 1 (e.g., a file path)
- REPLACE — Input 2 (e.g., user instructions)

## Your Process

1. REPLACE — First step
2. REPLACE — Core processing
3. REPLACE — Finalize and report

## Output Format

REPLACE — What to report back. Example:
"Field: value | Field: value | Key insight: one sentence"

## Rules

- REPLACE — Scope limits (what you must NOT do)
- REPLACE — Coordination rules (e.g., don't modify index — orchestrator does that)
- REPLACE — Quality bar (e.g., minimum detail level)
